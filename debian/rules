#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_install:
	dh_auto_install -- PREFIX=/usr

override_dh_auto_test:
	./panfan --version
	./panfan policy
	./panfan policy panfan.conf.example

override_dh_installsystemd:
	dh_installsystemd --no-enable --no-start panfan.service
